{
  // Rows an enabled oma-cursor-style plugin contributes to the Omarchy menu.
  // Merged between the shipped defaults and the user extension, so a user row
  // with the same id still wins.
  "style.cursor-style": {
    "icon": "󰇀",
    "label": "Cursor Style",
    "aliases": ["cursor", "cursor-picker"],
    "action": "omarchy-shell shell toggle taxin.cursor-style"
  }
}
